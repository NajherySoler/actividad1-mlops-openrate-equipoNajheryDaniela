data:
  processed_path: data/processed/openrate_processed.csv

split:
  test_size: 0.2
  random_state: 42

models:
  logistic_regression:
    max_iter: 2000
    random_state: 42

  random_forest:
    n_estimators: 100
    max_depth: 5
    random_state: 42

mlflow:
  tracking_uri: sqlite:///mlflow.db
  experiment_name: openrate_classification